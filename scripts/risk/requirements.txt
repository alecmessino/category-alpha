# Millibar Risk Evidence pipeline (scripts/risk).
#
# Pinned to what the record is actually built and gated against, in the same spirit as
# scripts/genesis/requirements.txt. A clean checkout plus this file reproduces the web
# note, the one-page PDF and every gate; nothing in the build reads an undeclared file.
shapely==2.1.2
pypdf==6.19.0
pytest==9.1.1
playwright==1.63.0
